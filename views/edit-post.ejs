<!DOCTYPE html>
<html lang="en">
  <%- include('includes/head.ejs') %>
  <body>
    <%- include('includes/header.ejs') %>
    <main>
      <form action="/edit-post/<%= post.id %>" method="POST">
        <% errors.forEach( error => { %>
        <p class="notice"><%= error %></p>
        <% }) %>

        <fieldset>
          <legend>Edit Existing Post</legend>
          <label for="title"> <small>Title</small> </label>
          <input id="title" value="<%= post.title %>"  name="title" type="text" autocomplete="off" />
          <label for="body"> <small>Content</small> </label>
          <textarea
            id="body"
            name="body"
            type="text"
            autocomplete="off"
          ><%= post.body %></textarea>
          
          <br />
          <button>Save Changes to Post</button>
        </fieldset>
      </form>
    </main>
    <%- include('includes/footer.ejs') %>
  </body>
</html>

